---
# yaml-language-server: $schema=../../docs-resources/schemas/param-defs-schema.json

$schema: "../../docs-resources/schemas/param-defs-schema.json#"

# Used to create parameters for cmo.adoc file.
# Tags refer to anchors in any adoc file for any ISA manual.

chapter_name: Cache Manage Ops

parameter_definitions:
  - name: CACHE_BLOCK_SIZE
    long-name: Cache Block Size
    impl-def: CACHE_BLOCK_SIZE
    type: uint
    width: 64
    description: The observable size of a cache block, in bytes.
