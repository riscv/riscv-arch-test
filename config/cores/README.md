# ACT Configuration Files for Running Tests on RISC-V Cores

These configuration files serve as examples for running the ACTs on a DUT. These configurations are specific to certain RISC-V cores, but they can be used as a starting point for many DUTs.

Each subdirectory contains configurations for a specific RISC-V core and may include multiple variants of that core.
