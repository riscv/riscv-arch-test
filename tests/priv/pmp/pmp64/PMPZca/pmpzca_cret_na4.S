// This file was generated by testgen (generators/testgen/src/testgen/priv/pmp); do not edit by hand.
// --------------------------------------------------------------------------------
// Copyright (C) 2025 Harvey Mudd College & Oklahoma State University, UET Lahore, Habib University
// SPDX-License-Identifier: Apache-2.0
//
// Title       : Comprehensive PMP (Physical Memory Protection) Verification
// Authors     : Umer Shahid, Allen Baum, David Harris
//               Muhammad Abdullah, Hamza Ali, Muhammad Zain
//
// Description : This test verifies the functionality and enforcement of
//               Physical Memory Protection (PMP) configurations in RISC-V
//               systems. It specifically tests the Read, Write, and Execute
//               permissions for a designated memory region, ensuring that
//               the PMP settings are correctly applied and that the system
//               behaves as expected when accessing this region.
//
// Coverpoints : cp_cret_na4 for PMPZca is fully covered in this test file.
//
// Test Cases  : Checking that 16-bit fetches adjacent to a NA4 PMP boundary succeed.
//               Set up a standard NA4 PMP region with L=1, XWR = 111. Placing four
//               c.ret = c.jr ra statements just below, at bottom, at top, and just
//               above PMP region, half of which are on 16-bit boundaries.
//               Attempt jalr to each c.ret.
// --------------------------------------------------------------------------------

##### START_TEST_CONFIG #####
# REQUIRED_EXTENSIONS:  ['Zca','Sm']
# params:
#   MXLEN: 64
#   NUM_PMP_ENTRIES: '>0'
#   PMP_NA4_SUPPORTED: true
# MARCH: rv64i_zicsr_zifencei_zca
##### END_TEST_CONFIG #####

// Define the size of the signature region
#define SIGUPD_COUNT 20

// Include framework macros
#include "riscv_arch_test.h"

// Set up test environment and declare main entry point
RVTEST_BEGIN

main:
    // Clear every pmpcfg and pmpaddr CSR
    .set pmpcfgi, CSR_PMPCFG0
    .rept UDB_NUM_PMP_ENTRIES/8
    csrw pmpcfgi, x0
    .set pmpcfgi, pmpcfgi+2
    .endr
    .set pmpaddri, CSR_PMPADDR0
    .rept UDB_NUM_PMP_ENTRIES
    csrw pmpaddri, x0
    .set pmpaddri, pmpaddri+1
    .endr

#define PMPCFG (((PMP_L|PMP_R|PMP_W|PMP_X|PMP_NA4)&0xFF) << PMP0_CFG_SHIFT)
#define REGIONSTART TEST_FOR_EXECUTION_1

    RVTEST_PMP_SET_BACKGROUND x4

// ---------------------------- Verification Section ----------------------------

// Test Case: NA4 region with L -> 1 and XWR -> 111, c.ret just below, at the bottom, at the top and just above it
    LA(x5, REGIONSTART)
    srl x5, x5, PMP_SHIFT
    csrw pmpaddr0, x5
    LI(x4, PMPCFG)
    csrw pmpcfg0, x4
    RVTEST_SFENCE_VMA_IF_SUPPORTED

    PMP_VERIFICATION_X_C TEST_FOR_EXECUTION_0, test1

    PMP_VERIFICATION_X_C TEST_FOR_EXECUTION_1, test2

    PMP_VERIFICATION_X_C TEST_FOR_EXECUTION_2, test3

    PMP_VERIFICATION_X_C TEST_FOR_EXECUTION_3, test4

    j exit

exit:

RVTEST_CODE_END
RVTEST_DATA_BEGIN

// Test case strings for reporting
test_1_str: .string "\"test: 1; cp: pmpzca_cret_na4_execute\""

.p2align 10
.skip 0x802
TEST_FOR_EXECUTION_0:
    ret
TEST_FOR_EXECUTION_1:
    ret
TEST_FOR_EXECUTION_2:
    ret
TEST_FOR_EXECUTION_3:
    ret
    .option push
    .option norvc
RETURN_INSTRUCTION:
    nop
    nop
    jr ra
    .option pop

RVTEST_DATA_END

// Signature region for self-checking
RVTEST_SIG_SETUP
