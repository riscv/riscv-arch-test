// PMPF Coverpoints.  Converted from the "PMPF" tab of the PMP test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1VuBt-H9VVWh8k-USAJEplrgEJETjQ-iNjD4Iq_ZOnN0/edit?gid=1472001893#gid=1472001893) on 2026-09-19.

[[t-PMPF-coverpoints]]
.PMPF Coverpoints
[cols="2,2,3,1", options=header]
|===
| Coverpoint | Goal | Feature Description | Bins

4+| See PMPM for general information. +
Tests run in M mode. +
In this suite, loads = flw, fld, flh +
stores = fsw, fsd, fsh +
Some loads/stores throw illegal instruction trap if not supported

| cp_cfg_RW
| The R, W, ...bits, when set, indicate that the PMP entry permits read, write
| Check that WR bits control write/read access for every type of load and store. +
mstatus.FS = nonzero. Attempt all types of reads and writes with pmpcfg_i.L=1, all legal pmpcfg_i.XWR +
Observe proper access faults for restricted read/write regions
| 6 XWR * (3 loads + 3 stores)

|===
