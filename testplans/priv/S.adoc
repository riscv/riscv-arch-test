// S Coverpoints.  Converted from the "S" tab of the Priv test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/10NX0T4MgueqA4p7JNg4whCq2k3YZ9R73UCV88MtcIUA/edit?gid=335048477#gid=335048477) on 2026-09-19.

[[t-S-coverpoints]]
.S Coverpoints
[cols="2,1,3,2,3", options=header]
|===
| Coverpoint | Goal | Feature Description | Expectation | Bins

| cp_scsr_access
|
| csr read, write all 1s, write all 0s, set all 1s, clear all 1s, write back inital value of each register below
|
| csrrs and csrrc to each bit

| cp_scsrwalk
| Walking 1s of legal supervisor CSRs
| Set and clear each bit of each writable S-mode CSR to exercise bitfields: sstatus, scause, sie, stvec, scounteren, senvcfg, sscratch, sip, excluding bit 33 of senvcfg as it results in an invalid PMM field.  sepc and stval are not tested because it is hard to express what values are legal.
|
| Illegal Instruction

| cp_csr_ro
|
| Write read-only CSRs (C00-EFF)
|
| Turning on satp.mode without having page tables will hang the system

| cp_satp
| Read satp
| Read satp
| Reads when 0, illegal instruction when 1.  Value is unpredictable because it is not reset and has WARL fields that might not even support Bare.
|

| cp_ucsr_from_s
|
| In supervisor mode, read/write/set/clear from each U-mode CSR if F/V are supported
|
|

| cp_csrr_insufficient_priv
|
| Read Machine CSRS 300-3FF, 700-7FF, B00-BFF, F000-FFF, excluding 7AA (mscontext)
|
|

| cp_scause_write
| Write scause values +
(WLRL)
| Write scause values: +
with interrupt = 1: 0-15 +
with interrupt = 0: 0-64
|
|

| cp_sstatus_sd_write
| Writing to the CSR field +
(WARL)
| Write 1 and 0 to SD with FS/VS/XS all 0.  Write cross-product of all bits of SD/ FS/ VS/XS.  128 bins of writes.
|
| Illegal instruction exception

| cp_sprivinst
|
| Execute ecall and ebreak and mret and and sfence.vma in supervisor mode
|
| Changes affecting coverpoints: +
moved scsr accesses from M to Sm +
duplicated cp_sret_s to Sm and stripped TSR out of the S version. + +
sepc should be pointing two instructions ahead.

| cp_sret_s
|
| Execute sret in supervisor mode with cross-product of all values of sstatus.SPP/SPIE/SIE.  Return to supervisor mode after each sret.
|
| sepc should be pointing two instructions ahead.

|===

[NOTE]
====
* SXL tested with EndianS
* *** if other supervisor extensions are supported, ifdef them in to run in M-mode, such as sstc to test access to stimecmp.
====
