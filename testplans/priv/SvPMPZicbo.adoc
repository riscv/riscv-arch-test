// SvPMPZicbo Coverpoints.  Converted from the "SvPMPZicbo" tab of the Sv test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1LGwD2e01j4SN9tg85nt6gPwpp7nXyXMF7hqbw3jvXko/edit?gid=1563579846#gid=1563579846) on 2026-09-19.

[[t-SvPMPZicbo-coverpoints]]
.SvPMPZicbo Coverpoints
[cols="2,1,1,5,1,1,1,1", options=header]
|===
| Coverpoint | Feature | Sub Feature | Feature Description | Additional Feature info | Pass/Fail Criteria +
(checker) | Functional Coverage | Feature covered

| pmp.1
| Exception in VM
| PMP permissions on Physical Address
| If PTE has valid and non-reserved RWX permissions, but the translated Physical address (pte.ppn of leaf PTE + offset) does not have (r,w,x) PMP permissions, then accessing the translated Physical address would raise access fault exception. +
Test in superviosr and user privilege mode for PTE at all levels +
Test Environment: +
Set satp.mode = sv32, sv39, sv48 & sv57 +
Valid PTE, with no reserved encodings +
There should not be any page fault condition +
Test Cases: +
Remove {r,w} PMP permission of translated Physical Address in pmpcfg and acess it using Zicbom & Zicboz instructions.
| store/AMO access fault raised
| Check against RM
| Done
| Uncovered

| pmp.2
|
| PMP permissions on PTE
| If PTE does not have read PMP permissions, then accessing it during page table walk would raise access fault exception of the corresponding access type. +
Test in supervisor and user privilege mode for PTE at all levels +
Test Environment: +
Set satp.mode = sv32, sv39, sv48 and sv57 +
Valid PTE, with no reserved encodings +
There should not be any page fault condition +
Test Cases: +
Remove read PMP permission of PTE address in pmpcfg and execute Zicbom & Zicboz instructions (using a VA which accesses that PTE).
| store/AMO access fault raised
| Check against RM
| Done
| Uncovered

|===
