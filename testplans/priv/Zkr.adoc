// Zkr Coverpoints.  Converted from the "Zkr" tab of the Priv test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/10NX0T4MgueqA4p7JNg4whCq2k3YZ9R73UCV88MtcIUA/edit?gid=231840071#gid=231840071) on 2026-09-19.

[[t-Zkr-coverpoints]]
.Zkr Coverpoints
[cols="2,2,3,2,1", options=header]
|===
| Coverpoint | Goal | Feature Description | Expectation | Bins

| cp_zkr_seed_csrrw
| seed CSR is readable and writable across allowed privilege modes
| Execute csrrw rd, seed, rs1 crossed with {privilege mode = M, S, U for supported modes} crossed with {mseccfg.sseed = 0/1, mseccfg.useed = 0/1.
| No exception when access is allowed
| 3 modes * 4 mseccfg = 12 bins

| cp_zkr_seed_illegal_csr_op
| Non-CSRRW CSR instructions on seed cause illegal instruction
| For each op in {CSRRS, CSRRC, CSRRW, CSRRWI, CSRRSI, CSRRCI} crossed with rs1/imm in {0/1} crossed with {mode = M, S (sseed=1), U (useed=1)} for supported modes: attempt the instruction on the seed CSR.
| An illegal instruction exception is raised for every effective read (CSRRS/C with x0 or imm 0), regardless of privilege mode or access control settings.
| 6 ops * 2 rs1 * 3 modes = 30 bins

| cp_zkr_seed_entropy_zero_non_es16
| # When OPST is not ES16, entropy must be 0.
| Execute csrrw rd, seed, rs1 +
Execute csrrw rd, seed, rs1 again +
check that if OPST is not ES16, sigupd entropy +
if OPST is ES16, sigupd 0x0BADBEEF
| entropy is 0 if OPST is not ES16
| 1

|===
