// ZawrsSm Coverpoints.  Converted from the "ZawrsSm" tab of the Zawrs test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1LpZQ1LwAInN2jjq_oDn_r8xIuPU7h_R_SpbgVbmUf54/edit?gid=1449589527#gid=1449589527) on 2026-09-19.

[[t-ZawrsSm-coverpoints]]
.ZawrsSm Coverpoints
[cols="2,1,1,3,2,1,3", options=header]
|===
| Coverpoint | Test Status | Goal | Feature Description | Expectation | Bins | Normative Rule

7+| covergroup Zawrs

7+| tests for single core

| cp_wrs_sto_timeout
|
| WRS.STO waits till timeout
| cross lr instruction to set up reservation. +
mstatus.TW = {0/1} +
mstatus.MIE = 0 +
mie=all 0s to disable interrupts +
Execute WRS.STO in M mode
| no exception, next instruction executes normally after WRS.STO finish execution
| 2 bins
| Zawrs_wrs-sto_stall_duration

| cp_wrs_no_res
|
| WRS behaves as nop when no reservation is established
| mstatus.TW ={0/1} +
mstatus.MIE = 0 +
mie=all 0s to disable interrupts +
Clear all reservation with sc.w, then execute {WRS.STO, WRS.NTO} with no reservation created in M mode
| WRS behaves as nop
| 2 x 2 bins
|

| cp_wrs_resume
|
| WRS follows the rules of WFI for resuming on interrupts
| cross lr instruction to set up reservation. +
mstatus.TW = {0/1} +
mstatus.MIE = {0/1} +
cross with mie.MTIE=1 +
Set up timer to interrupt soon +
execute {WRS.NTO/WRS.STO} in M mode
| WRS resumes when interrupt is pending, MIE should not affect result
| 2 x 2 x 2 bins
| Zawrs_exec_resume_rules + +
LA(t0, zawrs_resume_trap_handler) +
csrrw t0, mtvec, t0 # save old trap vector +
1: wrs.nto  # keep doing nto until timeout +
if MIE=0 and mip.MTIP, exit with success +
j 1b +
zawrs_resume_trap_handler: +
csrr t1, mcause +
SIGUPD t1 +
csrw stvec, t0 # restore trap handler +
done:

| cp_wrs_no_mie
|
| WRS does not stop when mie = 0 for pending interrupt
| cross lr instruction to set up reservation +
mstatus.MIE = 1 +
mie = all 0s +
mstatus.TW = 0 +
mip.mtip = {MSIP + MEIP + MTIP} +
execute WRS.STO in M mode
| WRS does not stop when interrupt pending
| 1 bin
|

7+| Can not be tested with single core:

| norm
| Zawrs_wrs-nto_stall_exec
|
|
|
|
|

|===
