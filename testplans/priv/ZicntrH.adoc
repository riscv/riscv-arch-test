// ZicntrH Coverpoints.  Converted from the "ZicntrH-JG" tab of the Hypervisor test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1DWBEoprndjQzLTXQTzPI8jW1HydtkYRR3tg58q59fdk/edit?gid=2079344243#gid=2079344243) on 2026-09-19.

[[t-ZicntrH-coverpoints]]
.ZicntrH Coverpoints
[cols="2,1,3,2,1,2", options=header]
|===
| Coverpoint | Goal | Feature Description | Expectation | Bins | ID

6+| in M mode

| cp_mhscounteren_access_m
| vscounters
| Write walking 1s and 0s to all of mcounteren, hcounteren, scounteren.  Read from corresponding counter and counterh in M-mode (should always succeed)
| always succeeds
|
| Reading back counter is part of test program but not coverpoint

| cp_delta
| htimedelta does not affect time in M mode
| With htimedelta = {0, 2^30, 2^60, -2^30, -2^60}, read time.   In RV32, this also involves htimedeltah and timeh. Note: do not check time direclty against reference model, which might tick differently.  Only look at the range of values.
| 0 < time < 2^30 in all circumstances
| 5
| norm:csr:htimedleta:delta

6+| in HS mode

| cp_scounteren_access_hs
| vscounters
| Write walking 1s and 0s to scounteren with mcounteren = all 1s, hcounteren = all 0s.  Read from corresponding counter and counterh in HS-mode (should always succeed)
| always succeeds
|
| Reading back counter is part of test program but not coverpoint

| cp_delta
| htimedelta does not affect time in HS mode
| With htimedelta = {0, 2^30, 2^60, -2^30, -2^60}, read time.   In RV32, this also involves htimedeltah and timeh. Note: do not check time direclty against reference model, which might tick differently.  Only look at the range of values.
| 0 < time < 2^30 in all circumstances
| 5
|

6+| in VS mode

| cp_mcounteren_access_vs
| vscounters
| Write walking 1s and 0s to mcounteren with hcounteren = all 1s and scounteren = all 0s.  Read from corresponding counter and counterh in VS-mode (should fail when 0)
| fails when 0
|
| Reading back counter is part of test program but not coverpoint

| cp_hcounteren_access_vs
| vscounters
| Write walking 1s and 0s to hcounteren with mcounteren = all 1s  and scounteren = all 0s.  Read from corresponding counter and counterh in VS-mode (should fail when 0)
| fails when 0
|
| Reading back counter is part of test program but not coverpoint

| cp_delta
| htimedelta affects time in VS mode
| With htimedelta = {0, 2^30, 2^60, -2^30, -2^60}, read time.   In RV32, this also involves htimedeltah and timeh. Note: do not check time direclty against reference model, which might tick differently.  Only look at the range of values.
| 0: 0 < time < 2^30 +
2^30: 2^30 < time < 2^60 +
2^60: 2^60 < time ^ 2^61 +
-2^30: -2^30 < time < 0 +
-2^60: -2^60 < time < 2^-59
| 5
|

6+| in U mode

| cp_scounteren_access_u
| vscounters
| Write walking 1s and 0s to scounteren with mcounteren = all 1s, hcounteren = all 0s.  Read from corresponding counter and counterh in U-mode (should fail when 0)
| fails when 0
|
| Reading back counter is part of test program but not coverpoint

| cp_delta
| htimedelta does not affect time in U mode
| With htimedelta = {0, 2^30, 2^60, -2^30, -2^60}, read time.   In RV32, this also involves htimedeltah and timeh. Note: do not check time direclty against reference model, which might tick differently.  Only look at the range of values.
| 0 < time < 2^30 in all circumstances
| 5
|

6+| in VU mode

| cp_hcounteren_access_vu
| vscounters
| Write walking 1s and 0s to hcounteren with mcounteren = scounteren = all 1s.  Read from corresopnding counter and counterh in VU-mode (should fail when 0)
| fails when 0
|
| Reading back counter is part of test program but not coverpoint

| cp_scounteren_access_vu
| vscounters
| Write walking 1s and 0s to scounteren with mcounteren = hcounteren = all 1s.  Read from corresopnding counter and counterh in VU-mode (should fail when 0)
| fails when 0
|
|

| cp_delta
| htimedelta affects time in VU mode
| With htimedelta = {0, 2^30, 2^60, -2^30, -2^60}, read time.   In RV32, this also involves htimedeltah and timeh. Note: do not check time direclty against reference model, which might tick differently.  Only look at the range of values.
| 0: 0 < time < 2^30 +
2^30: 2^30 < time < 2^60 +
2^60: 2^60 < time ^ 2^61 +
-2^30: -2^30 < time < 0 +
-2^60: -2^60 < time < 2^-59
| 5
|

|===

[NOTE]
====
* NOTE: xcounteren are WARL and some bits could be read-only zero.  Currently, the coverpoints won't be hit.  What should we do about this?
* *** move everything related to htimedelta from ZicsrH to here
====
