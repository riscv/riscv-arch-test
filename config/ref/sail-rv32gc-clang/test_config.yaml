name: sail-rv32gc-clang
compiler_exe: clang # executable name on $PATH or full path to executable
objdump_exe: llvm-objdump # executable name on $PATH or full path to executable, optional
ref_model_exe: sail_riscv_sim # executable name on $PATH or full path to executable
udb_config: sail-rv32gc.yaml
linker_script: link.ld
dut_include_dir: . # Directory containing DUT specific model_test.h header
