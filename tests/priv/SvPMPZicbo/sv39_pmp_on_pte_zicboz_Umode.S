// This file was generated by testgen (generators/testgen/src/testgen/priv/sv); do not edit by hand.
//---------------------------------------------------------------------------------------------------------------------------------
// Developed by: Umer Shahid & Muhammad Zain
//---------------------------------------------------------------------------------------------------------------------------------
// Test cases are as follows:
//---------------------------------------------------------------------------------------------------------------------------------
// 1. Page table holding the level 2 PTE is an X-only NAPOT PMP region:
//        Then, in U-Mode, the cache-block operations run --> required: 1 Store access fault(s)
// 2. Page table holding the level 1 PTE is an X-only NAPOT PMP region:
//        Then, in U-Mode, the cache-block operations run --> required: 1 Store access fault(s)
// 3. Page table holding the level 0 PTE is an X-only NAPOT PMP region:
//        Then, in U-Mode, the cache-block operations run --> required: 1 Store access fault(s)
//
// Total Expected Faults :: 3
//---------------------------------------------------------------------------------------------------------------------------------

##### START_TEST_CONFIG #####
# REQUIRED_EXTENSIONS: [I, Sv39, Zicboz, Sm]
# params:
#   NUM_PMP_ENTRIES: '>0'
# MARCH: rv64i_zicsr_zifencei_zicboz
##### END_TEST_CONFIG #####

#define SIGUPD_COUNT 15
#define TRAP_SIGUPD_COUNT 30
#define BOOT_TO_MMODE


#include "riscv_arch_test.h"

RVTEST_BEGIN

//---------------------------------------------------------------------------------------------------------------------------------
// Test cbo.zero
.macro VERIFICATION_RWX

  cbo.zero (a5)
  nop

.endm

.macro TEST_CASES_RUNNER LOWER_MODE, VA, level
  // Adjust offset of VA to point to test region
  .set VA_SHIFT, (\level*9)+12
  LI(a5, (\VA>>VA_SHIFT)<<VA_SHIFT)
  LA(a0, rvtest_data_1)
  slli a0, a0, 64-VA_SHIFT
  srli a0, a0, 64-VA_SHIFT
  add  a5, a5, a0             // Virtual Address

  RVTEST_GOTO_LOWER_MODE \LOWER_MODE

  VERIFICATION_RWX

  RVTEST_GOTO_MMODE

.endm

// PMP Macros
#define PMP0CFG_X             (((PMP_NAPOT | PMP_X) & 0xFF) << PMP0_CFG_SHIFT)


main:

  RVTEST_PMP_SET_BACKGROUND t2

//---------------------------------------------------------------------------------------------------------------------------------
//                                   Virtual addresses definition for code and test regions
//---------------------------------------------------------------------------------------------------------------------------------

  // Virtual Address of Test section (rvtest_data_1)
  .set va_data,               0x00000000

  // Virtual Address for code region
  .set va_rvtest_code_begin,  0xFFFFFFFF80000000


  // PTE setup for Code Region
  SUPERPAGE_PTE_SETUP_SV39(rvtest_code_begin, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_R | PTE_V), va_rvtest_code_begin, LEVEL2)
  sfence.vma

  // Save area setup for code region
  csrr a0, mscratch
  SAVE_AREA_SETUP(va_rvtest_code_begin, rvtest_code_begin, code, LEVEL2)

//---------------------------------------------------------------------------------------------------------------------------------
//                                               Test Cases Start from here
//---------------------------------------------------------------------------------------------------------------------------------

  SATP_SETUP_RV64(sv39)                                                    // Set SATP for virtualization
  sfence.vma                                                               // Flush TLB

  LI(t0, MENVCFG_CBZE)
  csrs menvcfg, t0
  csrs senvcfg, t0

//---------------------------------------------------------------------------------------------------------------------------------
//---------------------------------------------------------------------------------------------------------------------------------
//                                                        TESTS AT LEVEL 2
//---------------------------------------------------------------------------------------------------------------------------------
//                  1GB PAGE    Region 1 under test at level 2 -- RWX permissions given to the region
//---------------------------------------------------------------------------------------------------------------------------------

  LA (t2, rvtest_Sroot_pg_tbl)
  srl t2, t2, PMP_SHIFT
  LI (t1, ((1<<(UDB_PMP_GRANULARITY - 2))-1)>>1)    // Minimum range is 8 Bytes (1 PTE). Increases as G increases
  or t2, t2, t1
  csrw pmpaddr0, t2

  LI (t2, PMP0CFG_X)
  write_pmpcfg0:
  RVTEST_SIGUPD_CSR_WRITE(pmpcfg0, t2, write_pmpcfg0, write_pmpcfg0_str)
  sfence.vma
  .if (UDB_PMP_GRANULARITY < 12)
  // Test case 1: Test in U-Mode | RWX set | expected = 1 Store access fault(s)
  SUPERPAGE_PTE_SETUP_SV39(rvtest_data_1, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_W | PTE_R | PTE_V), va_data, LEVEL2)
  sfence.vma

  TEST_CASES_RUNNER Umode, va_data, LEVEL2
  .endif

//---------------------------------------------------------------------------------------------------------------------------------
//---------------------------------------------------------------------------------------------------------------------------------
//                                                        TESTS AT LEVEL 1
//---------------------------------------------------------------------------------------------------------------------------------
//                  2MB PAGE    Region 1 under test at level 1 -- RWX permissions given to the region
//---------------------------------------------------------------------------------------------------------------------------------

  LA (t2, rvtest_slvl1_pg_tbl)
  srl t2, t2, PMP_SHIFT
  LI (t1, ((1<<(UDB_PMP_GRANULARITY - 2))-1)>>1)    // Minimum range is 8 Bytes (1 PTE). Increases as G increases
  or t2, t2, t1
  csrw pmpaddr0, t2

  sfence.vma

  // Test case 2: Test in U-Mode | RWX set | expected = 1 Store access fault(s)
  PTE_SETUP_SV39(rvtest_slvl1_pg_tbl, (PTE_V), va_data, LEVEL2)
  SUPERPAGE_PTE_SETUP_SV39(rvtest_data_1, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_W | PTE_R | PTE_V), va_data, LEVEL1)
  sfence.vma

  TEST_CASES_RUNNER Umode, va_data, LEVEL1

//---------------------------------------------------------------------------------------------------------------------------------
//---------------------------------------------------------------------------------------------------------------------------------
//                                                        TESTS AT LEVEL 0
//---------------------------------------------------------------------------------------------------------------------------------
//                  4KB PAGE    Region 1 under test at level 0 -- RWX permissions given to the region
//---------------------------------------------------------------------------------------------------------------------------------

  LA (t2, rvtest_slvl0_pg_tbl)
  srl t2, t2, PMP_SHIFT
  LI (t1, ((1<<(UDB_PMP_GRANULARITY - 2))-1)>>1)    // Minimum range is 8 Bytes (1 PTE). Increases as G increases
  or t2, t2, t1
  csrw pmpaddr0, t2

  sfence.vma

  // Test case 3: Test in U-Mode | RWX set | expected = 1 Store access fault(s)
  PTE_SETUP_SV39(rvtest_slvl1_pg_tbl, (PTE_V), va_data, LEVEL2)
  PTE_SETUP_SV39(rvtest_slvl0_pg_tbl, (PTE_V), va_data, LEVEL1)
  PTE_SETUP_SV39(rvtest_data_1, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_W | PTE_R | PTE_V), va_data, LEVEL0)
  sfence.vma

  TEST_CASES_RUNNER Umode, va_data, LEVEL0


//---------------------------------------------------------------------------------------------------------------------------------
RVTEST_CODE_END
RVTEST_DATA_BEGIN

//---------------------------------------------------------------------------------------------------------------------------------
//                                            PHYSICAL ADDRESS REGION FOR TESTING
//---------------------------------------------------------------------------------------------------------------------------------
// Physical Address region under testing for LEVEL 0, 1 and 2
.p2align 12
rvtest_data_1:
  nop
  addi a4, a2, 4
  jr ra
  nop
  .word 0xbeefcaf1          // Random word
  .word 0xbeefcaf2          // Random word
  nop
  jr ra

//---------------------------------------------------------------------------------------------------------------------------------

// Page Tables
.p2align 12
.p2align (UDB_PMP_GRANULARITY)
rvtest_slvl0_pg_tbl:
    .skip(4096)
.p2align (UDB_PMP_GRANULARITY)
rvtest_slvl1_pg_tbl:
    .skip(4096)
.p2align (UDB_PMP_GRANULARITY)

write_pmpcfg0_str: .string "\"Mismatch in pmpcfg0!\""

RVTEST_DATA_END
RVTEST_SIG_SETUP
