# yaml-language-server: $schema=testplan.schema.json
suite: Ssu64xl
source: https://docs.google.com/spreadsheets/d/10NX0T4MgueqA4p7JNg4whCq2k3YZ9R73UCV88MtcIUA/edit?gid=75338461#gid=75338461
coverpoints:
  - name: cp_ssu64xl
    goal: UXLEN = 64 supported
    description: With sstatus.UXL = 2, in U-mode, set bit 63 of a GPR and read back
    expectation: bit 63 is set
    bins: "1"
    rules:
      - sstatus.UXL must be capable of holding the value 2 (i.e., UXLEN=64 must be supported)
