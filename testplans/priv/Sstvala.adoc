// Sstvala Coverpoints.  Converted from the "Sstvala" tab of the Priv test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/10NX0T4MgueqA4p7JNg4whCq2k3YZ9R73UCV88MtcIUA/edit?gid=691634508#gid=691634508) on 2026-09-19.

[[t-Sstvala-coverpoints]]
.Sstvala Coverpoints
[cols="2,2,1,2,1,4", options=header]
|===
| Coverpoint | Goal | Feature Description | Expectation | Bins | Normative Rule

6+| All tests take place in S-mode with exceptions delegated to S-mode.

| cp_stval_load_page_fault
| stval is written with faulting VA
| With VM enabled, cause load page fault.
| fault, stval written with VA
| 1
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_store_page_fault
| stval is written with faulting VA
| With VM enabled, cause store page fault.
| fault, stval written with VA
| 1
|

| cp_stval_instr_page_fault
| stval is written with faulting VA
| With VM enabled, cause instruction page fault.
| fault, stval written with VA
| 1
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_load_access_fault
| stval is written with faulting VA
| load from an illegal address
| fault, stval written with VA
| 1
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_store_access_fault
| stval is written with faulting VA
| store to an illegal address
| fault, stval written with VA
| 1
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_instr_access_fault
| stval is written with faulting VA
| jump to an illegal address
| fault, stval written with VA
| 1
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_load_misaligned_fault
| stval is written with faulting VA if misaligned fault occurs
| lw with misaligned address
| succeeed, or fault with stval containing address
|
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_store_misaligned_fault
| stval is written with faulting VA if misaligned fault occurs
| sw with misaligned address
| succeeed, or fault with stval containing address
|
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_instr_misaligned_fault
| stval is written with faulting VA if misaligned fault occurs
| jalr with misaligned address
| succeeed, or fault with stval containing address
|
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

| cp_stval_illegal_instr
| stval is written with the illegal instruction
| Execute 0xFFFFFFFF, 0x00000000
| Illegal instruction fault with stval = 0xFFFFFFFF, 0x00000000
| 2
| stval must be written with the faulting virtual address for load, store, and instruction page-fault, access-fault, and misaligned exceptions, and for breakpoint exceptions other than those caused by execution of the ebreak or c.ebreak instructions. For illegal-instruction exceptions, stval must be written with the faulting instruction.

|===

[NOTE]
====
* Note: breakpoints tested in SstvalaSdtrig because they are generated with debug triggers
====
