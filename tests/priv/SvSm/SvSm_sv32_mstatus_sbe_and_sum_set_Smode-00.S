///////////////////////////////////////////
// SvSm/SvSm_sv32_mstatus_sbe_and_sum_set_Smode-00.S
//
// This file is autogenerated by testgen.
// Do not modify directly.
//
// Copyright (C) 2025 Harvey Mudd College & Oklahoma State University, UET Lahore, Habib University
// SPDX-License-Identifier: Apache-2.0
///////////////////////////////////////////

##### START_TEST_CONFIG #####
# REQUIRED_EXTENSIONS: ['I', 'Sm', 'Sv32', 'NORUN']
# # # no param constraints
# MARCH: rv${XLEN}i_zicsr_zifencei
##### END_TEST_CONFIG #####

// Define the size of the signature region
#define SIGUPD_COUNT 14

// Extra defines (if needed) to enable trap handlers, fpu, vector, etc.
#define BOOT_TO_MMODE
#define TRAP_SIGUPD_COUNT 10

// Include framework macros
#include "riscv_arch_test.h"

// Set up test environment and declare main entry point
RVTEST_BEGIN



/////////////////////////////////
// cp_sv32_mstatus_sbe_and_sum_set_Smode
/////////////////////////////////


main:

// Virtual addresses for code and test regions

// Virtual address of rvtest_data_1
  .set va_data, 0x90407000

// Virtual address of the code region
  .set va_rvtest_code_begin, 0x30000000

// Map the code region.
  SUPERPAGE_PTE_SETUP_SV32(rvtest_code_begin, (PTE_D | PTE_A | PTE_X | PTE_R | PTE_V), va_rvtest_code_begin, LEVEL1)
  sfence.vma

// Register the alias so the T-SBI GOTO macros can move an excursion into it.
  LA(a0, Mtramptbl_sv)
  SAVE_AREA_SETUP(va_rvtest_code_begin, rvtest_code_begin, code, LEVEL1)

  SATP_SETUP_SV32 // Enable address translation.
  sfence.vma

  LI(   t0, MSTATUSH_SBE)
  csrs  mstatush, t0
  LI(   t0, MSTATUS_SUM)
  csrs  mstatus, t0

  LI(a2, 0x800) // Test signature initialization

  li a3, 0
  li a4, 0
  li a5, 24
  .rept(4)
  andi a3, a0, 0xFF
  srli a0, a0, 8
  sll a3, a3, a5
  or a4, a4, a3
  addi a5, a5, -8
  .endr
  SREG a4, 0(t1)
// The S-mode accesses run from the boot identity map, so its root PTE must be big-endian too.
  LA(t0, rvtest_code_begin)
  srli t0, t0, 22
  andi t0, t0, 0x3FF
  slli t0, t0, 2
  LA(t1, rvtest_Sroot_pg_tbl)
  add t1, t1, t0
  LREG a0, 0(t1)
  li a3, 0
  li a4, 0
  li a5, 24
  .rept(4)
  andi a3, a0, 0xFF
  srli a0, a0, 8
  sll a3, a3, a5
  or a4, a4, a3
  addi a5, a5, -8
  .endr
  SREG a4, 0(t1)

// 4MB page at level 1

// Test case 1: Big-endian PTE | Test in S-Mode | RWX bit set | expected = No Fault
  SUPERPAGE_PTE_SETUP_SV32(rvtest_data_1, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_W | PTE_R | PTE_V), va_data, LEVEL1)
  li a3, 0
  li a4, 0
  li a5, 24
  .rept(4)
  andi a3, a0, 0xFF
  srli a0, a0, 8
  sll a3, a3, a5
  or a4, a4, a3
  addi a5, a5, -8
  .endr
  SREG a4, 0(t1)
  sfence.vma

  LI(a5, (va_data >> 22) << 22)
  LA(a0, rvtest_data_1)
  slli a0, a0, 10
  srli a0, a0, 10
  add a5, a5, a0
  RVTEST_TSBI_GOTO_SMODE
  addi a2, a2, 16

// Store
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_store:
  sw a2, 20(a5)
  nop

// Load
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_load:
  lw a3, 20(a5)
  nop

  RVTEST_TSBI_GOTO_MMODE

  # Check if x12 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x12, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_store, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_store_str)
  # Check if x13 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x13, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_load, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_load_str)


// 4KB page at level 0

// Test case 2: Big-endian PTE | Test in S-Mode | RWX bit set | expected = No Fault
  PTE_SETUP_SV32(rvtest_slvl0_pg_tbl, (PTE_V), va_data, LEVEL1)
  li a3, 0
  li a4, 0
  li a5, 24
  .rept(4)
  andi a3, a0, 0xFF
  srli a0, a0, 8
  sll a3, a3, a5
  or a4, a4, a3
  addi a5, a5, -8
  .endr
  SREG a4, 0(t1)
  PTE_SETUP_SV32(rvtest_data_1, (PTE_SOFT | PTE_D | PTE_A | PTE_U | PTE_X | PTE_W | PTE_R | PTE_V), va_data, LEVEL0)
  li a3, 0
  li a4, 0
  li a5, 24
  .rept(4)
  andi a3, a0, 0xFF
  srli a0, a0, 8
  sll a3, a3, a5
  or a4, a4, a3
  addi a5, a5, -8
  .endr
  SREG a4, 0(t1)
  sfence.vma

  LI(a5, va_data)
  RVTEST_TSBI_GOTO_SMODE
  addi a2, a2, 16

// Store
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_store:
  sw a2, 20(a5)
  nop

// Load
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_load:
  lw a3, 20(a5)
  nop

  RVTEST_TSBI_GOTO_MMODE

  # Check if x12 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x12, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_store, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_store_str)
  # Check if x13 contains the expected result. x2 is the signature ptr, x5 is the link ptr, x4 is a temp reg.
  RVTEST_SIGUPD(x2, x5, x4, x13, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_load, SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_load_str)


// Instantiate trap handlers and other epilog code, call RVMODEL_HALT at end of test
RVTEST_CODE_END

// Create scratch data sections and label for test data
RVTEST_DATA_BEGIN

// Test data

// Physical address region for testing
// Physical address region for levels 0 and 1
  .p2align 12
rvtest_data_1:
  nop
  addi a4, a2, 4
  jr ra
  nop
  .word 0xbeefcaf1
  .word 0xbeefcaf2
  nop
  jr ra

// Page tables
  .p2align 12
rvtest_slvl0_pg_tbl:
  .skip(4096)

// Testcase strings
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_store_str: .string "\"test: 7; cg: SvSm; cp: cp_sv32_mstatus_sbe_and_sum_set_Smode; bin: test1_store\""
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test1_load_str: .string "\"test: 8; cg: SvSm; cp: cp_sv32_mstatus_sbe_and_sum_set_Smode; bin: test1_load\""
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_store_str: .string "\"test: 9; cg: SvSm; cp: cp_sv32_mstatus_sbe_and_sum_set_Smode; bin: test2_store\""
SvSm_cp_sv32_mstatus_sbe_and_sum_set_Smode_test2_load_str: .string "\"test: 10; cg: SvSm; cp: cp_sv32_mstatus_sbe_and_sum_set_Smode; bin: test2_load\""

// End of test data section and model specific data
RVTEST_DATA_END

// Signature region for self-checking
RVTEST_SIG_SETUP
