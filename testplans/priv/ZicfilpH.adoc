// ZicfilpH Coverpoints.  Converted from the "ZicfilpH" tab of the cfi test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1ED9ontJZP-Dg2MAF-4mkhqKFgpjf6Fp6yeXKuW61FOY/edit?gid=1715431862#gid=1715431862) on 2026-09-19.

[[t-ZicfilpH-coverpoints]]
.ZicfilpH Coverpoints
[cols="2,2,3,2,2,1,1", options=header]
|===
| Coverpoint | Goal | Feature Description | Expectation | Bins | ID | Normative Rule

7+| General Test Environment: +
(VS/VU-mode, H-extension) +
same base environment as ZicfilpU plus H-extension implemented. +
menvcfg.LPE=1 and henvcfg.LPE=1 unless stated otherwise. +
VU additionally requires senvcfg.LPE=1. +
G-stage translation active (hgatp.MODE != Bare) unless stated otherwise.

| cp_elp_state_preservation_zicflip_vs
| Verify trap in VS-mode saves and restores previous ELP via vsstatus.VSPELP
| Test Environment: +
Execute an indirect branch in VS-mode with rs1 != {x1,x5,x7} to set ELP = LP_EXPECTED. +
Trigger a trap using ECALL. With virtual supervisor delegation enabled, trap is delivered to VS-mode, updating vsstatus.VSPELP with the previous ELP.
| Trap delivered to VS-mode, vsstatus.VSPELP stores previous ELP value, after executing VSRET, ELP is restored to LP_EXPECTED
| Origin mode (VS-mode) × Trap target (VS-mode) × ELP state
| sstatus-spelp_op
| [norm:sstatus-spelp_op]

|===
