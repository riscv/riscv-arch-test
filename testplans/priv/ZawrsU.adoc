// ZawrsU Coverpoints.  Converted from the "ZawrsU" tab of the Zawrs test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1LpZQ1LwAInN2jjq_oDn_r8xIuPU7h_R_SpbgVbmUf54/edit?gid=1437737576#gid=1437737576) on 2026-09-19.

[[t-ZawrsU-coverpoints]]
.ZawrsU Coverpoints
[cols="2,1,3,2,1,2,1", options=header]
|===
| Coverpoint | Goal | Feature Description | Expectation | Bins | Comments | Normative Rule

7+| covergroup Zawrs

7+| tests for single core

| cp_wrs_sto_timeout
| WRS.STO waits till timeout
| cross lr instruction to set up reservation. +
mstatus.TW = {0/1} +
mstatus.MIE = 0 +
mstatus.SIE = 0 +
mie=all zeros 0 to disable interrupts +
Execute WRS.STO in U mode
| no exception, next instruction executes normally after WRS.STO finish execution
| 2 bins
|
| Zawrs_wrs-sto_stall_duration

| cp_wrs_no_res
| WRS behaves as nop when no reservation is established
| mstatus.TW =0 +
mstatus.MIE = 0 +
mstatus.SIE = 0 +
mie=all 0s to disable interrupts +
Clear all reservation with sc.w, then execute {WRS.STO, WRS.NTO} with no reservation created in U mode
| WRS behaves as nop
| 2 bins
| TW = 1 can not be tested because the DUT may trigger illegal instruction immediately, where SAIL allows the instruction to retire successfully
|

| cp_wrs_resume
| WRS follows the rules of WFI for resuming on interrupts
| cross lr instruction to set up reservation. +
mstatus.TW = 0 +
cross with mie.MTIE=1 +
(if SSTC supporrted use STIP, cross menvcfg.STCE = 1) +
mstatus.MIE = {0/1} +
(if S supported:mstatus.SIE = {0/1}) +
Set up timer to interrupt soon +
execute {WRS.NTO/WRS.STO} in U mode
| WRS resumes when interrupt is pending, MIE should not affect result, trap in all cases other than in S mode with SIE = 0 (still resumes)
| 2 x 2 x 2 bins
|
| Zawrs_exec_resume_rules

| cp_wrs_nto_timeout
| WRS.NTO timeout when TW = 1
| cross lr instruction to set up reservation. +
mstatus.TW = 1 +
mstatus.MIE = 0 +
mstatus.SIE = 0 +
mie=all 0s to disable interrupts +
execute WRS.NTO in U mode
| WRS causes illegal instruction exception when timed out if TW = 1
| 1
|
| Zawrs_priv_illegal_instr_excp

| cp_wrs_no_mie
|
| cross lr instruction to set up reservation +
mstatus.MIE = 1 +
mstatus.SIE = 1 +
mie = all 0s +
mstatus.TW = 1 +
mip.mtip = {SSIP + SEIP + STIP + MSIP + MEIP + MTIP} +
execute WRS.NTO and WRS.STO in U mode
| WRS does not stop when interrupt pending, WRS.NTO causes illegal instruction
| 2 bins
|
|

|===
