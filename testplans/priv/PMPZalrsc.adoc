// PMPZalrsc Coverpoints.  Converted from the "PMPZalrsc" tab of the PMP test plan spreadsheet
// (https://docs.google.com/spreadsheets/d/1VuBt-H9VVWh8k-USAJEplrgEJETjQ-iNjD4Iq_ZOnN0/edit?gid=613653829#gid=613653829) on 2026-09-19.

[[t-PMPZalrsc-coverpoints]]
.PMPZalrsc Coverpoints
[cols="2,2,3,2,1", options=header]
|===
| Coverpoint | Goal | Feature Description | Notes | Bins

5+| See PMPM for general information. +
Tests run in M mode. +
In this suite, loads = lr.w, stores = sc.w +
RV64 adds lr.d, sc.d

| cp_cfg_RW
| The R, W, ...bits, when set, indicate that the PMP entry permits read, write
| Check that WR bits control write/read access for every type of load and store. +
Attempt all types of reads and writes with pmpcfg_i.L=1, all legal pmpcfg_i.XWR +
Observe proper access faults for restricted read/write regions
| For stores that are expected to succeed, a constrained LRSC loop must be used
| 6 XWR * (2-4 ops)

|===
